import React from 'react'
import styled from 'styled-components'

const Footer = styled.div<React.FC>`
    height: 70px;
    border-top: 1px solid rgba(0, 0, 0, 0.2);
    background: #FFFFFF;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 15px;
`

const Styled = {
    Footer
}

export default Styled